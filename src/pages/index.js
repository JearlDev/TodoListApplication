import * as React from 'react';

import ListManager from '../components/ListManager';

const IndexPage = () => {
  return (
    <main className="main">
      <ListManager />
    </main>
  );
};

export default IndexPage;
